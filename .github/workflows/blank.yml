# 1. 进入项目目录
cd Open-AutoGLM-Hybrid

# 2. 初始化 Git 仓库
git init

# 3. 添加所有文件
git add .

# 4. 提交
git commit -m "Initial commit"

# 5. 添加远程仓库 (替换为您的仓库地址)
git remote add origin https://github.com/mzxdsg-hub/Open-AutoGLM-Hybrid.git

# 6. 推送代码
git branch -M main
git push -u origin main
